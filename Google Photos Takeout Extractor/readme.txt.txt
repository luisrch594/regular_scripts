To execute this file, first install Python, any Python should work.


then run 

python extract_files_from_google_photos_backup.py 


This will create a new directory with today's date and time.

It must be run inside a Takeout directory or any directory that has a Google Fotos or Google Photos directory inside it. 